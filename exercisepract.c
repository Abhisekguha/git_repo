#include <stdio.h>

int main() {
int arr[] = {10, 20, 30, 40, 50};  // declare an integer array
int i;

printf("Elements of the array are:\n");

    // loop to print the elements of the array
for(i = 0; i< 5; i++) {
printf("%d\n", arr[i]);
    }

return 0;
}

